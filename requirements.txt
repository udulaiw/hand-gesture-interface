# Python 3.12
#
# Pinned to the API surfaces this project actually targets. mediapipe 1.x
# removed the legacy `mp.solutions` module, so the code uses the Tasks
# HandLandmarker; do not downgrade below 1.0 without changing tracking/.
mediapipe>=1.0,<2
opencv-python>=4.8
numpy>=1.24

# Not required. Rendering is done entirely with OpenCV drawing primitives on a
# single 8-bit overlay, which avoids a second windowing system and its input
# handling. Left here only as a note for anyone expecting it.
# pygame>=2.6
